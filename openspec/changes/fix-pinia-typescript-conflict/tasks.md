## 1. Update TypeScript Version

- [x] 1.1 Update `frontend/package.json`: Change `typescript` version from `~5.3.3` to `^5.6.0`.

## 2. Install Dependencies

- [x] 2.1 Run `cd frontend && npm install` to update dependencies and lock file.

## 3. Verify Build

- [x] 3.1 Run `cd frontend && npm run build` and verify compilation succeeds without errors.

## 4. Verify Tests

- [x] 4.1 Run `cd frontend && npm run test` and verify tests pass (if any exist).

## 5. Commit Changes

- [ ] 5.1 Commit `frontend/package.json` and `frontend/package-lock.json` (or `pnpm-lock.yaml` if using pnpm).